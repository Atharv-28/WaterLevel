import { View, StyleSheet, SafeAreaView, Text  } from "react-native";
import Tank from "../components/tank";

const Home = () => {
    return(
        <SafeAreaView>
            <Tank />
        </SafeAreaView>
    );
}


export default Home;